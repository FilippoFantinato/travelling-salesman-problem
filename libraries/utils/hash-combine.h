#ifndef LIN_KERNIGHAN_HASH_COMBINE_H
#define LIN_KERNIGHAN_HASH_COMBINE_H

#include <cstddef>

namespace Utils {
    size_t hash_combine( size_t lhs, size_t rhs );
}

#endif //LIN_KERNIGHAN_HASH_COMBINE_H
